# Screenshot assets for README and docs.

## portal-catalog.png

Public catalog homepage (`/`).

- **Theme:** dark (default) or light — use the sun/moon toggle in the header
- **Recommended size:** ~1440×900
- **Show:** hero banner, at least one registry section (Docker / Maven / NPM)

## dashboard.png (optional)

Admin dashboard (`/dashboard`) — light content area, dark sidebar.

## login.png (optional)

Sign-in screen (`/login`) — dark `zinc-900` layout.
